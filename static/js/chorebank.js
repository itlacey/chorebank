/**
 * ChoreBank -- minimal custom JS
 *
 * AdminLTE 4 handles sidebar toggle and layout.
 * HTMX handles interactive updates.
 * This file is for ChoreBank-specific behavior.
 */

document.addEventListener("DOMContentLoaded", function () {
    // Future: HTMX event handlers for timer, chore completion, etc.
    // htmx events: htmx:afterSwap, htmx:beforeRequest, etc.
});
